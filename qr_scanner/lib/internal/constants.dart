class AppConstants {
  static const API_URL = 'http://ems.iotmind.vn/api';
  static const helperSSID = "SSID is the technical term for a network name. "
      "When you set up a wireless home network, "
      "you give it a name to distinguish it from other networks in your neighbourhood.";
  static const helperBSSID =
      "BSSID is the MAC address of the wireless access point (router).";
  static const helperPassword = "The password of the Wi-Fi network";
}
